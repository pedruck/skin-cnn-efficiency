# Google Colab already ships: torch, torchvision, numpy, pandas, scikit-learn,
# matplotlib, seaborn, tqdm. The floors below are what the code was written against;
# `pip install -r requirements.txt` on Colab only needs to add thop / kagglehub / pyyaml.

torch>=2.1
torchvision>=0.16
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
pyyaml>=6.0
tqdm>=4.65
thop>=0.1.1.post2209072238
kagglehub>=0.3.0

# optional: GPU power / energy logging during training (skipped if missing)
nvidia-ml-py>=12.535.108
